# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'

use_frameworks!

target 'WhenWePlay' do

    # UI
    pod 'Reusable'
    pod 'Cartography'
    pod 'DeepDiff'

    # API
    pod 'Contentful', '~> 1.0.0-beta5'

    # Test
    pod 'SkeletonView'


    target 'WhenWePlayTests' do
        inherit! :search_paths
        # Pods for testing
    end

end
